{% extends 'base.html.twig' %}

{% block title %}Hello CoursesController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

    {% if app.user %}
        <div class="mb-3">
            You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
        </div>
    {% else %}
        <div class="mb-3">
            <a href="{{ path('app_login') }}">Sign in</a>
        </div>
        <div class="mb-3">
            <a href="{{ path('app_register') }}">Sign up</a>
        </div>
    {% endif %}

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ '/var/www/symfony/src/Controller/CoursesController.php'|file_link(0) }}">src/Controller/CoursesController.php</a></code></li>
        <li>Your template at <code><a href="{{ '/var/www/symfony/templates/courses/index.html.twig'|file_link(0) }}">templates/courses/index.html.twig</a></code></li>
    </ul>
</div>
{% endblock %}
